<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.thomasmore.flinkspreken">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".FrontStopActivity" />
        <activity android:name=".NewAccount" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".FinaalInitiaalActivity" />
        <activity android:name=".DoelklankActivity" />
        <activity android:name=".LuisterGoedActivity" />
        <activity android:name=".ZegHetZelfEensActivity" />
        <activity android:name=".SpelKiezenActivity" />
        <activity android:name=".ZegHetZelfEens" />
        <activity android:name=".PaarActivity"></activity>
    </application>

</manifest>